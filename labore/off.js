// Calling the add function with specific values
let result = add(3, 5);
console.log(result); // Output: 8

// You can also directly use variables as arguments
let a = 10, b = 20;
console.log(add(a, b)); // Output: 30
